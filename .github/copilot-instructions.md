- [x] Verify repository instructions exist.
- [x] Clarify requirements: follow the supplied Elite Fabric ontology workshop brief.
- [x] Scaffold the requested multi-script Python repository; keep README intent first.
- [x] Customize with deterministic data, ontology, REST deployment, and evaluation.
- [x] Install required extensions: none required for this multi-script template.
- [x] Compile and test with the repository Python 3.12 virtual environment and pinned dependencies.
- [x] Create and run task: use cross-platform repository entry-point scripts instead.
- [x] Launch: live cloud execution requires configured tenant credentials; never invent them.
- [x] Complete documentation with measured local results and explicit unsupported preview gaps.

Use a fixed as-of date and one seeded random generator. Keep each firm's rows isolated.
Never treat agent instructions as authorization. Never invent Fabric endpoints or claim
offline fixtures are live responses. Preserve owned-resource state for safe teardown.